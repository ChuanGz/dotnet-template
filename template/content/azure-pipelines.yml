trigger: [main]
pool:
  vmImage: ubuntu-latest
steps:
  - task: UseDotNet@2
    inputs:
      version: 10.0.x
  - script: dotnet restore && dotnet format --verify-no-changes --no-restore && dotnet build -c Release --no-restore
  - script: docker build -t generated-service:verify .
